services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: peremetours
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - peremetours-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d peremetours"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  peremetours-postgres:
